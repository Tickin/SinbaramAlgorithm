#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main(void){
	/*****/
	freopen("sample.inp", "r", stdin);
	/*****/

	int T, testCase;
	scanf("%d", &T);

	for (testCase = 0; testCase < T; testCase++){

	}
}